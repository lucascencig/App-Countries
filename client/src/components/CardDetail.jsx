import React from "react";
import { Link } from "react-router-dom";

export default function CardDetail(flags, name, idPais, continente, capital, subregion, area, poblacion, actividadTuristica){
    return(
        <div>
            <div>
                
            </div>
        </div>

    )
}